

// export const path$ = (p: string) => (getLocale() === "en" ? "" : `/${getLocale()}`) + p;

// export const localizedPath$ = (p: string, locale = getLocale()) =>
//     locale === "en" ? p : `/${locale}${p}`;